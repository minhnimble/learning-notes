# Purpose of the Rails Router

Rails router jobs are:
- Matches incoming HTTP requests to specific controller actions in your Rails application based on the URL path.
- Generates path and URL helpers based on the resources configured in the router.